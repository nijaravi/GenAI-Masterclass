# Core
pydantic>=2.7
pydantic-settings>=2.2
fastapi>=0.110
uvicorn[standard]>=0.29

# LLM + embeddings
openai>=1.30           # used for OpenAI AND Groq (OpenAI-compatible API)
tiktoken>=0.7

# Retrieval
rank-bm25>=0.2.2
sentence-transformers>=2.7   # local embeddings + cross-encoder reranker
numpy>=1.26

# Vector stores (install the one you use; both listed for convenience)
chromadb>=0.5
psycopg[binary]>=3.1
pgvector>=0.2.5

# Dev / test
pytest>=8.0
httpx>=0.27
