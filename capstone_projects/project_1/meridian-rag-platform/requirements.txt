# ---- LangChain stack (pinned to the 0.3 line taught in the course) ----
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4          # loaders, BM25Retriever, cross-encoder, cache
langchain-text-splitters>=0.3,<0.4     # MarkdownHeaderTextSplitter, RecursiveCharacterTextSplitter
langchain-openai>=0.2,<0.4             # ChatOpenAI, OpenAIEmbeddings
langchain-groq>=0.2,<0.4               # ChatGroq (Llama)
langchain-chroma>=0.1,<0.3             # Chroma vector store integration

# ---- supporting libraries the LangChain components rely on ----
chromadb>=0.5            # vector store backend
rank-bm25>=0.2           # backs BM25Retriever
tiktoken>=0.7            # token-accurate splitting (RecursiveCharacterTextSplitter.from_tiktoken_encoder)

# ---- backend / frontend ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
streamlit>=1.33
httpx>=0.27

# ---- optional: cross-encoder reranking (heavy). Without it, rerank step is skipped. ----
# sentence-transformers>=2.7

# ---- dev / test ----
pytest>=8.0
