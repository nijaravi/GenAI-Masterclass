# TechStore Support API — Section 9
# pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.0.0

# LLM
openai>=1.30.0

# Vector store (runs in-process, no separate server)
chromadb==0.5.3

# Utilities
python-dotenv==1.0.1
